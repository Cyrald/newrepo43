# Инструкция по запуску проекта ЭкоМаркет на Windows

## Требования к системе

- **Windows** 10/11
- **Процессор**: 2+ ядра
- **ОЗУ**: минимум 4 ГБ
- **Место на диске**: минимум 2 ГБ свободного места

---

## Шаг 1: Установка Node.js

1. Перейдите на официальный сайт Node.js: https://nodejs.org/
2. Скачайте **LTS версию** (Long Term Support) для Windows
3. Запустите скачанный установщик `node-vXX.X.X-x64.msi`
4. Следуйте инструкциям установщика:
   - Примите лицензионное соглашение
   - Выберите путь установки (по умолчанию `C:\Program Files\nodejs\`)
   - Оставьте все опции включенными (установка npm, добавление в PATH)
5. После установки откройте **Командную строку** (Win+R → `cmd` → Enter)
6. Проверьте установку:
   ```bash
   node --version
   npm --version
   ```
   Должны появиться версии Node.js и npm (например, `v20.11.0` и `10.2.4`)

---

## Шаг 2: Установка PostgreSQL

### Вариант A: Установка PostgreSQL локально

1. Скачайте PostgreSQL: https://www.postgresql.org/download/windows/
2. Запустите установщик и следуйте инструкциям:
   - Запомните **пароль для пользователя postgres** (он понадобится!)
   - Порт по умолчанию: **5432**
   - Locale: можно оставить "Default locale"
3. После установки PostgreSQL установится вместе с pgAdmin (графический интерфейс)

### Вариант B: Использование Docker (опционально)

Если у вас установлен Docker Desktop для Windows:

```bash
docker run --name ecomarket-db -e POSTGRES_PASSWORD=mypassword -e POSTGRES_DB=ecomarket -p 5432:5432 -d postgres:15
```

---

## Шаг 3: Создание базы данных

### Через pgAdmin (графический интерфейс):

1. Откройте **pgAdmin** из меню Пуск
2. Подключитесь к серверу PostgreSQL (введите пароль, который установили)
3. Правый клик на **Databases** → **Create** → **Database...**
4. Введите имя базы данных: `ecomarket`
5. Нажмите **Save**

### Через командную строку (psql):

1. Откройте **Командную строку** (Win+R → `cmd`)
2. Перейдите в папку PostgreSQL:
   ```bash
   cd "C:\Program Files\PostgreSQL\15\bin"
   ```
3. Подключитесь к PostgreSQL:
   ```bash
   psql -U postgres
   ```
4. Введите пароль postgres
5. Создайте базу данных:
   ```sql
   CREATE DATABASE ecomarket;
   \q
   ```

---

## Шаг 4: Скачивание проекта

### Вариант A: Скачать ZIP-архив

1. Если проект на GitHub, нажмите кнопку **Code** → **Download ZIP**
2. Распакуйте архив в удобное место (например, `C:\Projects\ecomarket`)

### Вариант B: Клонирование через Git

1. Установите Git: https://git-scm.com/download/win
2. Откройте **Git Bash** или **Командную строку**
3. Перейдите в папку для проектов:
   ```bash
   cd C:\Projects
   ```
4. Клонируйте репозиторий:
   ```bash
   git clone <URL_репозитория>
   cd ecomarket
   ```

---

## Шаг 5: Настройка переменных окружения

1. В корневой папке проекта создайте файл `.env` (без имени, только расширение)
   
   **Для создания в Windows:**
   - Откройте **Блокнот** (Notepad)
   - Вставьте следующий текст:
   
   ```env
   # База данных
   DATABASE_URL=postgresql://postgres:ВАШ_ПАРОЛЬ@localhost:5432/ecomarket
   
   # JWT секрет (можно оставить как есть или изменить на случайную строку)
   JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
   
   # Email (опционально, для верификации email)
   SMTP_HOST=smtp.gmail.com
   SMTP_PORT=587
   SMTP_USER=your-email@gmail.com
   SMTP_PASSWORD=your-app-password
   SMTP_FROM=noreply@ecomarket.ru
   
   # Порт сервера (можно оставить по умолчанию)
   PORT=5000
   ```

   - **Сохраните файл**: Файл → Сохранить как...
   - В поле "Имя файла" введите: `.env` (с точкой в начале!)
   - В поле "Тип файла" выберите "Все файлы (*.*)"
   - Нажмите Сохранить

2. **ВАЖНО**: Замените `ВАШ_ПАРОЛЬ` на пароль от PostgreSQL

---

## Шаг 6: Установка зависимостей проекта

1. Откройте **Командную строку** или **PowerShell**
2. Перейдите в папку проекта:
   ```bash
   cd C:\Projects\ecomarket
   ```
3. Установите все зависимости:
   ```bash
   npm install
   ```
   
   Процесс займет 2-5 минут. Дождитесь завершения.

---

## Шаг 7: Создание схемы базы данных

После установки зависимостей создайте таблицы в базе данных:

```bash
npm run db:push
```

Вы должны увидеть сообщение:
```
✓ Changes applied
```

---

## Шаг 8: Заполнение базы данных тестовыми данными

Для тестирования запустите скрипт заполнения данных:

```bash
npx tsx server/seed.ts
```

Скрипт создаст:
- **4 пользователя** (включая администратора)
- **5 категорий** товаров
- **30 товаров** с описаниями и ценами
- **30 изображений** товаров

**Тестовые учетные данные:**
- **Администратор**: `admin@ecomarket.ru` / `admin123`
- **Пользователь 1**: `user1@example.com` / `user123`
- **Пользователь 2**: `user2@example.com` / `user123`
- **Пользователь 3**: `user3@example.com` / `user123`

---

## Шаг 9: Запуск проекта

### Режим разработки (с hot reload):

```bash
npm run dev
```

Сервер запустится на порту **5000**. Вы увидите сообщение:
```
serving on port 5000
```

### Режим продакшн (оптимизированная сборка):

1. Сначала соберите проект:
   ```bash
   npm run build
   ```

2. Затем запустите продакшн-сервер:
   ```bash
   npm run start
   ```

---

## Шаг 10: Открытие сайта в браузере

1. Откройте браузер (Chrome, Firefox, Edge)
2. Перейдите по адресу: **http://localhost:5000**

Вы должны увидеть главную страницу ЭкоМаркет!

---

## Возможные проблемы и решения

### ❌ Ошибка: "порт 5000 уже занят"

**Решение**: Измените порт в файле `.env`:
```env
PORT=3000
```
Затем откройте сайт по адресу: http://localhost:3000

---

### ❌ Ошибка: "не удается подключиться к базе данных"

**Проверьте:**
1. Запущен ли PostgreSQL (Диспетчер задач → Службы → найти postgresql)
2. Правильный ли пароль в `.env`
3. Существует ли база данных `ecomarket`

**Решение через pgAdmin:**
- Откройте pgAdmin
- Проверьте, что база данных `ecomarket` существует
- Проверьте, что сервер PostgreSQL запущен

---

### ❌ Ошибка: "node не является внутренней или внешней командой"

**Решение**: Node.js не добавлен в PATH. 
1. Переустановите Node.js
2. При установке убедитесь, что галочка "Add to PATH" включена
3. Перезагрузите компьютер после установки

---

### ❌ Ошибка: "npm ERR! permission denied"

**Решение для Windows**: Запустите Командную строку **от имени администратора**
1. Правый клик на "Командная строка"
2. "Запуск от имени администратора"
3. Повторите команду

---

### ❌ Изображения товаров не загружаются

**Решение**: Убедитесь, что папка `uploads` существует:
1. В корне проекта создайте папки:
   ```
   uploads/
   ├── products/
   └── chat/
   ```

---

## Полезные команды

```bash
# Проверка версий
node --version
npm --version

# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev

# Сборка проекта для продакшна
npm run build

# Запуск продакшн-версии
npm run start

# Проверка TypeScript (без запуска)
npm run check

# Обновление схемы базы данных
npm run db:push

# Заполнение тестовыми данными
npx tsx server/seed.ts
```

---

## Структура проекта

```
ecomarket/
├── client/              # Frontend (React)
│   ├── src/
│   │   ├── pages/       # Страницы
│   │   ├── components/  # Компоненты UI
│   │   ├── hooks/       # React Query хуки
│   │   └── stores/      # Zustand stores
│   └── public/          # Статика
├── server/              # Backend (Express)
│   ├── routes.ts        # API маршруты
│   ├── storage.ts       # Работа с БД
│   ├── auth.ts          # Аутентификация
│   └── seed.ts          # Заполнение данными
├── shared/              # Общие типы и схемы
│   └── schema.ts        # Drizzle ORM схемы
├── uploads/             # Загруженные файлы
├── package.json         # Зависимости проекта
├── .env                 # Переменные окружения
└── tsconfig.json        # Конфигурация TypeScript
```

---

## Дополнительная помощь

- **Документация Node.js**: https://nodejs.org/docs
- **Документация PostgreSQL**: https://www.postgresql.org/docs/
- **Документация React**: https://react.dev
- **Документация Express**: https://expressjs.com

---

**Готово! 🎉** 

Теперь у вас запущен полнофункциональный интернет-магазин натуральных продуктов!
